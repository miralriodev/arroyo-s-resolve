// Placeholder Service Worker for vite-plugin-pwa generateSW
// This file exists only to satisfy plugin build expectations.
// Actual precaching and runtime caching are generated via Workbox (generateSW).

self.addEventListener('install', () => {
  // no-op
})

self.addEventListener('activate', () => {
  // no-op
})